import React from "react";

const PageLoader: React.FC = () => {
  return (
    <div className="page__loader--wrapper">
      <span className="loader"></span>
    </div>
  );
};

export default PageLoader;
